package com.filesync.application.port.in;

// This interface might be merged or refined depending on how TransferJob entities are managed.
// For now, it can be a placeholder or include specific job-related operations not covered by FileTransferUseCase.
public interface TransferJobUseCase {
    // e.g., methods for direct manipulation or query of TransferJob if needed separately
}
