package com.filesync.application.port.out;

import com.filesync.domain.model.StorageLocation;
import java.util.List;
import java.util.Optional;

public interface StorageLocationRepositoryPort {
    // StorageLocation save(StorageLocation storageLocation);
    // Optional<StorageLocation> findById(String id);
    // List<StorageLocation> findAll();
    // void deleteById(String id);
    // Optional<StorageLocation> findByName(String name); // Example custom query
}
