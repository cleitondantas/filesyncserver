package com.filesync.application.port.out;

import com.filesync.domain.model.TransferJob;
import com.filesync.domain.model.TransferStatus;
// import org.springframework.data.domain.Page;
// import org.springframework.data.domain.Pageable;
import java.util.Optional;

public interface TransferJobRepositoryPort {
    // TransferJob save(TransferJob transferJob);
    // Optional<TransferJob> findById(String id);
    // Page<TransferJob> findAllByStatus(TransferStatus status, Pageable pageable);
    // Page<TransferJob> findAll(Pageable pageable);
    // Add other query methods as needed based on TransferJobUseCase requirements
}
